(()=>{const S=window.SCENARIOS,$=s=>document.querySelector(s);let mode="",score={love:0,trust:0,spark:0};function drawScore(a){if(a){score.love=Math.max(0,score.love+a[0]);score.trust=Math.max(0,score.trust+a[1]);score.spark=Math.max(0,score.spark+a[2])}$("#love").textContent=score.love;$("#trust").textContent=score.trust;$("#spark").textContent=score.spark}function play(id,why=""){const x=S[mode][id];drawScore(x.add);$("#name").textContent=x.name;$("#mood").textContent=x.mood;$("#avatar").textContent=x.avatar;$("#dialog").textContent=x.text;const an=$("#analysis");an.classList.toggle("hidden",!why);$("#analysisText").textContent=why;const box=$("#choices");box.innerHTML="";if(x.end){const d=document.createElement("div");d.className="analysis";d.textContent=`情境結束｜好感 ${score.love}・信任 ${score.trust}・心動 ${score.spark}`;box.appendChild(d);return}(x.choices||[]).forEach(([t,n,e])=>{const b=document.createElement("button");b.textContent=t;b.addEventListener("click",()=>play(n,e));box.appendChild(b)})}document.querySelectorAll("[data-mode]").forEach(b=>b.addEventListener("click",()=>{mode=b.dataset.mode;score={love:0,trust:0,spark:0};drawScore();$("#modes").classList.add("hidden");$("#game").classList.remove("hidden");$("#scores").classList.remove("hidden");play("start")}));$("#home").addEventListener("click",()=>{$("#modes").classList.remove("hidden");$("#game").classList.add("hidden");$("#scores").classList.add("hidden")});})();